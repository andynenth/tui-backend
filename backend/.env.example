# Liap Tui Backend Configuration

# Static files configuration
STATIC_DIR=backend/static
INDEX_FILE=index.html
ALLOWED_ORIGINS=*

# Structured Logging Configuration
# Set to "true" to enable file-based JSON logging (recommended for production)
LOG_TO_FILES=false

# Directory where log files will be written (only used if LOG_TO_FILES=true)
LOG_DIRECTORY=logs

# Example production configuration:
# LOG_TO_FILES=true
# LOG_DIRECTORY=/var/log/liap-tui